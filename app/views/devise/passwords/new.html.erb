<!DOCTYPE html>
<html lang = 'en'>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <title>Log In</title>
</head>
<body>
<div class="Forget-Pass-Box">
  <div class="Container">
    <%= image_tag "logo.png", class:"Logo-image" %> 
    <B><h2>Forgot your password?</h2></B>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-group">
        <%= f.label :email, class:"field-label" %><br>
        </div>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"field"%><br>

        <%= f.submit "Send me reset password instructions", class:"Button" %><br>
    <% end %>
<%= render "devise/shared/links" %>
  </div>
  </div>

</body>
</html>
