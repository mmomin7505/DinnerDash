<!DOCTYPE html>
<html lang='en'>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <title>Log In</title>
</head>
<body>
<div class="Box-container">
  <div class="row">
<div class="Box">
    <div class="form-container">
      <%= image_tag "logo.png", class: "Logo-image" %> 
      <h2><b>Login Form</b></h2>
      <%= render "layouts/flash_message" %>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-group">
          <%= f.label :email, class: "field-label" %><br />
        </div>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "field" %><br>

        <div class="form-group">
          <%= f.label :password, class: "field-label" %><br/>
        </div>
        <%= f.password_field :password, autocomplete: "current-password", class: "field" %><br>

        <% if devise_mapping.rememberable? %>
          <div class="form-group">
            <div class="Login-Rememberme-field">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
          </div>
        <% end %><br>

        <%= f.submit "Log in", class: "Submit-button" %><br><br>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
</div>
</body>
</html>
